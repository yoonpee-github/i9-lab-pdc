/** @type {import('next').NextConfig} */
// const nextConfig = {}

// module.exports = nextConfig


const nextConfig = {
  images: {
    domains: ['192.168.10.200'],
    // domains: ['localhost'],
  },
};

module.exports = nextConfig;
